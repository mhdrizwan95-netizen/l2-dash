# L2 HMM Trading System Environment Variables

# Rollback Feature Flags
# Set USE_LEGACY=true to enable 1-minute rollback to legacy dashboard
# This shows the old L2Dashboard at root path (/) for quick reversion
# Default is false (shows new cockpit)
USE_LEGACY=false

INGEST_KEY=CHANGE_ME
